module.exports = {
  definition: ``,
  query: `
    postByUrl(url: String!): Post
  `,
  mutation: ``,
  resolver: {
    Query: {
      posts: {
        description: 'posts graphql',
        resolver: {
          handler: 'Post.find'
        },
        policies: [
          'global.isAuthenticated'
        ]
      },
      post: {
        description: 'post graphql',
        resolver: {
          handler: 'Post.findByUrl'
        },
        policies: [
          'global.isAuthenticated'
        ]
      },
      postByUrl: {
        description: 'post by url graphql',
        resolver: {
          handler: 'Post.findByUrl'
        },
        policies: [
          'global.isAuthenticated'
        ]
      },
    }
  }
};

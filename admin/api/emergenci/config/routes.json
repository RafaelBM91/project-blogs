{
  "routes": [
    {
      "method": "GET",
      "path": "/emergencis",
      "handler": "Emergenci.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/emergencis/count",
      "handler": "Emergenci.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/emergencis/:id",
      "handler": "Emergenci.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/emergencis",
      "handler": "Emergenci.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/emergencis/:id",
      "handler": "Emergenci.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/emergencis/:id",
      "handler": "Emergenci.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
